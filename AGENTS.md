# AI操作规范（必须遵守）

## 核心原则
任何对本仓库的修改、新增、更新，都必须同步到GitHub，并在commit message中注明修改要点。

## 操作流程

### 1. 修改代码/配置
- 修改后立即 `git add` + `git commit` + `git push`
- commit message格式：`类型: 修改要点`
  - 示例：`feat: 新增web-js-extractor技能，支持多技能切换`
  - 示例：`fix: 修复校验模块中文变量名误判问题`
  - 示例：`docs: 更新技能文档，补充CDP调试实例规范`

### 2. 新增文件
- 新文件加入仓库后立即提交推送
- commit message注明新增了什么文件、用途是什么

### 3. 执行工作流时的查漏补缺
使用工作流生成代码过程中，发现以下情况必须更新技能文档：
- 技能文档规范有遗漏、矛盾、过时
- 发现更优、更快、更稳的实现方案
- 踩坑经验需要补充（接口参数变化、登录态坑、编码问题等）

更新技能文档时：
- 在文档对应章节补充或修订
- 在文档末尾「踩坑要点」或对应章节记录修改原因
- 告知用户本次修改要点
- 同步推送到GitHub

### 4. 需要修正时的确认
涉及以下情况必须先反问用户：
- 修改技能文档的核心规范（非补充性修改）
- 删除或重构现有功能
- 影响已有工作流兼容性的改动

反问格式：`本次修改涉及XX，是否需要同步到GitHub？`
用户确认后执行修改+推送，并备注原因。

## 禁止行为
- 禁止只修改不推送
- 禁止commit message写"更新"、"修改"等无意义描述
- 禁止发现技能文档问题不更新
- 禁止未经用户确认修改核心规范
